_site
backup
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
vendor

*.swp

# local preview only (node-specific paths)
_config_local.yml
serve-local.sh
